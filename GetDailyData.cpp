// GetDailyData.cpp
// Thomas Wise
// COSC 2030
// Project01
// 24 Oct 2018

#include "GetDailyData.h"

void getDailyData(longNumber& sum, longNumber& count, float& min, float& max)
{
	bool done = false;
	char command;
	longNumber temp;
	while (!done)
	{
		while (!(cin >> temp))
		{
			cin.clear();
			cin >> command;
			if (command == 'D')
			{

			}
		}
	}
}